# PMI Nowcasting 景气度即时预测系统 — 设计文档

> 日期:2026-07-15
> 定位:求职作品集 · 数字经济 / 宏观景气分析 + 机器学习
> 一句话:用一个央行真在用的方法(Nowcasting),把方法论的严谨性做到位,而不是靠堆技术栈。

---

## 0. 项目决策汇总

| 维度 | 决策 |
|------|------|
| **目的** | 求职实习作品集(需技术深度 + 完成度 + 能讲故事) |
| **方向** | 数字经济 / 宏观景气分析 + ML |
| **数据源** | akshare(免费、程序化拉取、可复现) |
| **预测目标** | 下月制造业 PMI 是否 > 50(荣枯线),二分类 |
| **模型** | 逻辑回归(baseline)+ 随机森林 / XGBoost(对比)+ naive baseline |
| **核心卖点** | 方法论严谨性:时点意识 / walk-forward 验证 / 防数据泄漏 / 经济价值评估 |
| **执行策略** | 核心层(必做,一定完成)+ 进阶层(高端加分,有余力再上) |

### 关于"高端"的定位
真正让量化 / 研究岗面试官眼睛一亮的高端,是**方法论的严谨性** —— 能识别别人踩的坑、用对的方法绕开它,而不是堆 LSTM / 微服务 / K8s。宏观数据只有一两百行,硬上重型技术栈只会显得不懂数据。

**"Nowcasting"** 是美联储、欧央行、IMF 真在用的术语与技术:用高频、抢先发布的数据来"抢跑"预测那些发布滞后的低频指标。项目名带这个词,面试官立刻知道你读过正经东西。

---

## 1. 项目架构 — 一个可复现的 Nowcasting 研究管线

### 三大方法论升级(项目"高端"的真正支点)

**升级 1:Point-in-Time(时点)数据意识 — 避免"未来函数"**
宏观数据有两个致命陷阱,90% 的新手项目都踩了,本项目主动规避并写进 README:
- **数据修订(vintage)**:PMI / GDP 发布后会被反复修正。若用"修正后的最终值"训练,等于偷看未来 → 回测虚高。
- **发布滞后(publication lag)**:预测 6 月 PMI 时,5 月社融可能还没发。特征只能用"那个时点真正拿得到的数据"。

国内数据源难拿完整 vintage,但**在代码里显式处理"发布滞后",并在 README 里讨论 vintage 问题**,这就是高端。

**升级 2:Walk-Forward(滚动前推)验证 + Purging / Embargo**
不是简单按时间切一刀,而是**扩展窗口滚动回测**;再加 López de Prado《Advances in Financial Machine Learning》里的 **purging 和 embargo**,防止训练集与测试集在时间上"泄漏"重叠。引用该书 = 量化圈加分项。

**升级 3:信号的经济学评估 — 不止准确率**
模型不只报 accuracy / AUC,还回答"**这个预警信号有没有经济价值**":按信号做简单择时(景气→持股指,收缩→持币),画净值曲线 vs 买入持有,算夏普比率。把"分类准确率"翻译成"钱",是量化面试的通用语言。

### 目录结构

```
pmi-nowcast/
├── data/
│   ├── raw/               # akshare 原始拉取(带拉取时间戳)
│   └── processed/         # 对齐、造好特征的训练表
├── src/pmi_nowcast/
│   ├── config.py          # 指标清单、发布滞后表、参数、路径
│   ├── ingest.py          # 拉数 + 缓存 + 记录数据"版本"
│   ├── calendar.py        # ★发布日历 / 滞后处理(升级1核心)
│   ├── features.py        # 滞后项、环比、扩散指数等特征
│   ├── dataset.py         # 组装训练表(严格时点对齐)
│   ├── validation.py      # ★walk-forward + purge/embargo(升级2)
│   ├── models.py          # LogReg / RF / XGBoost 统一接口
│   ├── train.py           # 训练 + 滚动回测 driver
│   ├── evaluate.py        # 分类指标 + SHAP 可解释性
│   └── backtest.py        # ★信号→择时净值曲线(升级3)
├── notebooks/             # EDA + 结果图
├── app.py                 # Streamlit 看板
├── tests/                 # 关键逻辑单测(尤其防泄漏的对齐逻辑)
├── .github/workflows/     # CI:跑测试(工程规范加分)
├── pyproject.toml
└── README.md              # 讲成一篇小研究报告
```

**关键分层原则**:`calendar.py` / `validation.py` / `backtest.py` 三个"高端模块"独立、可单测 —— 面试时可指着说"这块我专门处理了 XX 坑",职责边界清楚。

**核心数据流**:
`ingest`(取数缓存)→ `calendar` + `dataset`(时点对齐、造特征标签)→ `validation` + `train`(walk-forward 训练)→ `evaluate`(评估 + 可解释性)→ `backtest`(经济价值)→ README / 看板(讲故事)。

---

## 2. 数据、特征与标签 — 用"扩散指数"思维做特征

高端之处:**不是随便抓一堆指标丢进模型,而是按宏观经济学逻辑组织特征。** 被问"为什么选这些特征",要能从经济含义答,而不是"我觉得有用"。

### 2.1 特征指标清单(akshare 可取)

按经济逻辑分四组,每组代表一条传导链条 —— 这个分组本身就是讲故事的框架:

| 组别 | 指标(示例) | 经济含义 / 为什么能预警景气 |
|------|-----------|------------------------------|
| **① 领先/金融** | 社融存量同比、M1、M2、M1-M2 剪刀差、利率(SHIBOR / 国债收益率利差) | 货币金融是实体经济的"先行灯",信用扩张领先景气 |
| **② 实体活动** | 工业增加值、发电量、货运量、PPI | 直接反映当下生产热度 |
| **③ 需求/价格** | CPI、PPI、社零、进出口同比 | 需求和价格的冷热 |
| **④ 预期/情绪** | 上月 PMI 及其分项、股市指数月度收益 | 市场对未来的预期,自相关性强 |

- **[核心]** 先跑通 ①②③ 里各 2-3 个最稳定、akshare 一定拿得到的:社融、M2、工业增加值、CPI、PPI、上月 PMI。
- **[进阶]** 加"利差""剪刀差"等衍生特征(经济学家真在看的信号),以及分项 PMI。

### 2.2 特征工程 — 时序特有的处理(每条都是面试点)

- **[核心] 滞后特征(lag)**:预测 t+1 的 PMI,特征全部来自 t 及之前,造 `lag1, lag2, lag3`。
- **[核心] 同比/环比变换**:宏观数据多不平稳(有趋势),用同比(YoY)、环比(MoM)转成变化率,让序列平稳 —— 可讲的"平稳性处理"。
- **[核心] 严格时点对齐**:结合发布滞后表,每个特征按"预测时点真正可得"对齐(预测 6 月 PMI 时,4 月工业增加值可用、5 月可能还没发)。
- **[进阶] 滚动统计**:3 个月滚动均值 / 标准差,捕捉趋势和波动。
- **[进阶] 标准化**:滚动 z-score(均值方差只能用训练窗口算,否则泄漏未来 —— 细节体现严谨)。

### 2.3 标签构造

- **[核心] 主标签**:`y = 1 if PMI(t+1) > 50 else 0`(下月是否扩张)。
- **坑提醒**:PMI 长期在 49-51 反复横跳,50 附近样本"临界",类别可能不平衡也不好分。这不是缺点,而是 README 里可诚实讨论的点:"荣枯线附近预测天然困难,是景气拐点识别的固有难度。"
- **[进阶] 副标签**:方向标签 `PMI(t+1) > PMI(t)`(上行/下行),作为第二个任务对比。

### 2.4 数据量的现实

月度数据,2005 年至今约 240 个月,去掉滞后和缺失,有效样本约 **180-220 行**。**这就是坚决不用深度学习的原因**,也是"验证方法的严谨性"比"模型复杂度"重要得多的原因 —— 小样本上,方法论就是一切。README 里主动点明,反显懂行。

---

## 3. 建模与验证 — 项目的"技术脊梁"

模型本身不难,难在**验证方法的严谨**。这是最能讲的一节。

### 3.1 模型梯队(统一接口,便于对比)

- **[核心] Baseline:逻辑回归(带 L2 正则)** —— 立标杆,系数可解释("社融剪刀差每变动 1 个标准差,扩张概率变化多少")。
- **[核心] 主力:随机森林 / XGBoost** —— 看非线性和特征交互能否超过 baseline。
- **[核心] 朴素基准(naive baseline)** —— 必加!如"永远预测下月与本月同向"或"永远预测扩张"。ML 模型若打不过它,说明没学到东西。主动报告此对比 = 成熟研究者标志。
- **[进阶] 集成 / stacking**:LogReg + RF 概率融合。锦上添花,别一开始就做。

`models.py` 用统一接口(`.fit()` / `.predict_proba()`)包装,`train.py` 循环跑所有模型 —— "加新模型只需几行"= 可扩展的实验框架。

### 3.2 Walk-Forward 验证(核心中的核心)

**绝对不能用 sklearn 默认随机 K 折** —— 那会用未来预测过去。正确做法:

```
扩展窗口(expanding window)滚动前推:
 训练[2005..2018]    → 预测[2019-01]
 训练[2005..2019-01] → 预测[2019-02]
 训练[2005..2019-02] → 预测[2019-03]
 ... 每期重新训练,窗口不断扩大 ...
```

- **[核心]** 用 sklearn `TimeSeriesSplit` 起步,或自己写扩展窗口循环(自己写更能讲清原理)。
- **[进阶] Purging + Embargo**:因造了 lag / 滚动特征,训练集末尾与测试集在时间上"沾边"→ 泄漏。**Purging** = 剔除与测试期重叠的训练样本;**Embargo** = 训练 / 测试间留缓冲间隔。引用 López de Prado。这是把项目从"学生作业"抬到"准专业"的关键。

`validation.py` 独立成模块、写单元测试(用构造假数据验证"测试期数据绝不进训练集")—— 该测试本身即面试亮点。

### 3.3 防泄漏总原则(README 单开一节)

集中列出所有"防未来函数"措施:时点对齐、滚动标准化只用训练窗口统计量、walk-forward、purge / embargo。**"我系统性地防范了数据泄漏"是量化面试的黄金句。**

---

## 4. 评估 — 从"准确率"到"钱"

三个层次,层层递进,每层都是讲故事素材。

### 4.1 [核心] 统计评估
- 分类指标:Accuracy、Precision / Recall、F1、**AUC-ROC**(不平衡数据看 AUC 比 accuracy 靠谱)。
- **混淆矩阵**:区分"漏报收缩"(危险)与"误报收缩"(踏空)的代价。
- 所有指标都在 **walk-forward 样本外**算,不是训练集 —— 反复强调。

### 4.2 [核心] 可解释性
- **特征重要性**:树模型 importance + 逻辑回归系数。
- **[进阶] SHAP 值**:画 SHAP summary plot,展示"哪个指标在哪个方向推动预警"。SHAP 是行业标准,会用 = 加分。
- 输出结论:"景气度最强的领先信号是 XX",呼应第 2 节经济逻辑。

### 4.3 [进阶] 经济价值回测(backtest.py)
把预测信号翻译成钱:
- 简单策略:信号=扩张则持有沪深300,收缩则持币(或持债)。
- 画**净值曲线 vs 买入持有**,算**年化收益、夏普比率、最大回撤**。
- 诚实提醒:仅示意"信号有无经济价值",非真实可交易策略(未算交易成本、滑点)。**主动声明局限 = 成熟。**

---

## 5. 交付物 — 让项目"看起来就很高端"

技术再好,别人 30 秒内看不懂就白搭。交付物决定第一印象。

### 5.1 [核心] README = 一篇迷你研究报告
1. **一句话摘要** + 一张最亮的图(净值曲线或 SHAP 图)
2. **问题背景**:什么是 Nowcasting,为什么预测 PMI 有价值
3. **数据与方法**:特征分组表、验证方法、防泄漏措施
4. **结果**:样本外指标 + 关键图表
5. **结论与局限**:最强信号是什么、模型打没打过 naive baseline、有哪些坑没解决
6. **如何复现**:`pip install` → 一条命令跑通

能讲清"背景-方法-结果-局限"的 README,比多写 500 行代码都值钱。

### 5.2 [进阶] Streamlit 交互看板(app.py)
- 展示:最新一期预测(扩张 / 收缩概率)、历史预测 vs 实际、特征贡献图、净值曲线。
- 部署到 **Streamlit Community Cloud(免费)**,README 放 live demo 链接 —— 面试官可直接点开玩。

### 5.3 [进阶] 一张"数据流 / 方法论"架构图
用 draw.io 或 mermaid 画:数据→时点对齐→特征→walk-forward→评估→回测。放 README 顶部,专业感拉满。

---

## 6. 工程规范 — 藏在细节里的"专业度"

- **[核心]** `pyproject.toml` 管依赖、`src/` 布局、清晰模块划分。
- **[核心]** 关键逻辑**单元测试**(pytest),尤其防泄漏的时点对齐和 walk-forward 逻辑 —— "我给数据泄漏防范写了测试"是极强信号。
- **[进阶]** **GitHub Actions CI**:push 自动跑测试 + lint(ruff)。绿色 CI 徽章 = 专业。
- **[核心]** 数据缓存:akshare 拉的数据存 `data/raw/` 带时间戳,别每次重拉(接口不稳)。
- **[核心]** 清晰 commit 历史 + 有意义的 commit message。

---

## 7. 分层执行计划 — 保证一学期能交付

每个里程碑做完项目都是完整可展示的,不会做到一半烂尾:

| 阶段 | 内容 | 层级 | 产出 |
|------|------|------|------|
| **M1 数据管线** | ingest + 缓存 + 时点对齐 + EDA notebook | 核心 | 能拉数、有探索性分析图 |
| **M2 最小可用模型** | 核心特征 + LogReg + 简单时序切分 + naive baseline | 核心 | **第一个能跑通、能讲的完整项目** |
| **M3 严谨验证** | walk-forward + RF / XGB + 统计评估 + 特征重要性 | 核心 | 方法论到位的完整版 |
| **M4 讲故事** | README 研究报告 + 架构图 + 单元测试 | 核心 | **简历可放,面试能讲** |
| **M5 经济价值** | backtest 净值曲线 + SHAP | 进阶 | 量化味拉满 |
| **M6 产品化** | Streamlit 看板 + CI + 部署 | 进阶 | live demo,杀伤力最大 |

**关键**:做完 **M4 项目就已完整、够用**。M5 / M6 是"还有时间就上"的加分项。无论一学期投入多少,都有可交付版本。

---

## 8. 风险与诚实声明

- **数据可得性**:akshare 部分接口偶尔不稳或字段变动 → 缓存 + 容错重试,M1 阶段先确认每个指标真能拉到。
- **小样本**:180-220 行,模型易过拟合 → 靠正则、简单模型、严格样本外验证控制,并在 README 坦承。
- **无完整 vintage**:国内难拿修订前数据 → 只显式处理发布滞后,vintage 问题在 README 讨论而非假装解决。
- **回测非真实策略**:未算成本 / 滑点,仅示意经济价值,主动声明。

---

*本设计文档为 PMI Nowcasting 项目的实现依据,后续进入实现计划(writing-plans)阶段。*
